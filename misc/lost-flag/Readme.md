The challenge hints that the flag will be in the `partners` tab.

Viewing the HTML source of the page and scrolling down reveals an HTML comment, which is the flag.

Flag: `SUCTF{w0w_v3ry_h1dd3n_h7ml_c0mm3n7}`